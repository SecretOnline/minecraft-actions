{
  "name": "minecraft-actions",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:act": "act push --artifact-server-path .act-artifacts"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@types/node": "^24.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
